// not implemented, yet
interface ReservedStock {
    productId: string;
    totalQuantity: number;
    averagePrice: number; // average price between all orders
    orders: string[]; // IDs of boughts/orders
}